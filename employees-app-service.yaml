---
apiVersion: v1
kind: Service
metadata:
  name: employees-app
spec:
  type: LoadBalancer
  selector:
    app: employees
    tier: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
